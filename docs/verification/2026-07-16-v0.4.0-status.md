# v0.4.0 Verification Status

Date: 2026-07-16
Branch: `feature/acp-homecmd`
Classification: approved use-case parity verified

## Repository Gates

- `.venv/Scripts/python.exe -m pytest -q`: 57 passed.
- `.venv/Scripts/python.exe scripts/ci_check.py`: passing syntax, cyclomatic
  complexity, board lifecycle, required paths, links, and repository hygiene.
- `.venv/Scripts/python.exe -m compileall -q src scripts tests`: passing.
- `git diff --check`: passing with line-ending warnings only.
- CLI profile: 211,313 calls in 0.239 seconds; application `main` completed in
  0.006 seconds and import/model construction dominated startup.

## Live ACP-MCP Adapter

`python scripts/adapter_smoke.py` passed with the pinned compatibility pair:

```text
acp-mcp==0.4.2
acp-sdk==0.8.4
tools: list_agents, run_agent, resume_run_agent
executed command: system.python_version
```

The smoke performs MCP `initialize`, `tools/list`, and an actual `tools/call`.
The corrected ACP message contract accepts the SDK's `parts`, `created_at`, and
`completed_at` fields without requiring a non-SDK role.

## Live Bounded Worker

`python scripts/worker_smoke.py` passed against LM Studio at
`http://127.0.0.1:1234/v1`:

```text
tools: call_worker
worker_id: lmstudio.default
nonempty local-model output: true
prompt persisted in audit: false
```

The native worker endpoint has no reference to the registry or executor and
accepts no caller-selected backend URL.

## Live Vault Cards

`python scripts/vault_smoke.py` passed through the real CLI entry path:

```text
filesystem.write: passed
filesystem.read: passed
filesystem.list: passed
vault content persisted in audit: false
```

Traversal and absolute paths are rejected in tests. The escape test uses a
Windows directory junction when symlink creation is denied, so resolved-path
containment is exercised against a real reparse point.

## Parity Boundary

Parity means the approved outcomes, not restoration of unsafe endpoints:

- Retained: compact search, one-card disclosure, bounded run previews, log on
  demand, direct CLI, ACP, and MCP adapter compatibility.
- Restored: bounded LM Studio `call_worker` and Obsidian vault read/list/write.
- Added: fixed read-only Git status/diff cards.
- Intentionally excluded: raw `/execute`, unrestricted filesystem/MQTT routes,
  Docker coupling, caller-controlled interpreter source, and uninstalled
  MQTT/Ollama integrations.
- Deferred: cancellation remains unnecessary while command runs are synchronous.

## Remaining External Evidence

- GitHub Actions results are available only after this revision is pushed.
- Docker image build/runtime is not a release gate because this deployment is
  intentionally local and separate from Docker.
- The 57-test suite also passed in an isolated `uv --frozen` environment. The
  reused project `.venv` contains unrelated globally inherited packages with
  dependency conflicts, so its `pip check` result is not release evidence.
